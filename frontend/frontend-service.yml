apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: frontend-service
  name: frontend-service
spec:
  ports:
  - name: "5000"
    port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: frontend-service
  type: ClusterIP
  env:
   - name: BACKEND_URL
     value: http://backend-service:5001
status:
  loadBalancer: {}
